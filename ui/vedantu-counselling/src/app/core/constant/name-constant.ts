export interface NameConstant {
    id: string;
    name: string;
}

export const categoryFilter: NameConstant = { 
    id: "categoryFilter",
    name: "Category Filter"
}

export const genderFilter: NameConstant = { 
    id: "genderFilter",
    name: "Gender Filter"
}

export const quotaFilter: NameConstant = { 
    id: "quotaFilter",
    name: "Quota Filter"
}